[package]
name = "thermo-app"
version = "0.1.0"
edition = '2021'

[dependencies]
statig = "0.3.0"

[profile.dev]
lto = true
panic = "abort"

[profile.release]
lto = true
panic = "abort"

[profile.test]
lto = true
